@model WineRecommendationApp.Models.UserPreferences

@{
    ViewData["Title"] = "Рекомендації вин";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white text-center">
                    <h2>🍷 Знайдіть своє ідеальне вино</h2>
                    <p class="mb-0">Заповніть ваші вподобання і отримайте персональні рекомендації на основі ML</p>
                </div>
                <div class="card-body p-4">
                    <form asp-action="GetRecommendations" method="post">
                        
                        <div class="mb-4">
                            <label class="form-label fw-bold">🍇 Тип вина</label>
                            <select asp-for="WineType" class="form-select form-select-lg">
                                <option value="red">🍷 Червоне</option>
                                <option value="white">🥂 Біле</option>
                                <option value="all">🍾 Всі типи</option>
                            </select>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">🍬 Солодкість</label>
                            <select asp-for="Sweetness" class="form-select form-select-lg">
                                <option value="dry">Сухе</option>
                                <option value="medium">Напівсухе</option>
                                <option value="sweet">Солодке</option>
                            </select>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">🍾 Вміст алкоголю (%)</label>
                            <div class="row">
                                <div class="col-6">
                                    <label class="form-label small">Мінімум</label>
                                    <input asp-for="MinAlcohol" type="number" step="0.5" class="form-control" min="8" max="15" />
                                </div>
                                <div class="col-6">
                                    <label class="form-label small">Максимум</label>
                                    <input asp-for="MaxAlcohol" type="number" step="0.5" class="form-control" min="8" max="15" />
                                </div>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">🧪 Бажана кислотність (pH)</label>
                            <input asp-for="PreferredPH" type="number" step="0.1" class="form-control" min="2.5" max="4.0" />
                            <small class="form-text text-muted">Нижче = більш кисле (2.8-3.2), Вище = м'якше (3.3-3.8)</small>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">💰 Максимальна ціна (грн)</label>
                            <input asp-for="MaxPrice" type="number" class="form-control" min="100" max="2000" step="50" />
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                🔍 Отримати рекомендації (ML аналіз)
                            </button>
                        </div>
                    </form>
                </div>
            </div>
            
            <div class="alert alert-info mt-4 text-center">
                <strong>💡 Підказка:</strong> Використовується ML модель на основі 6500+ реальних вин
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border-radius: 15px;
    }
    .card-header {
        border-radius: 15px 15px 0 0 !important;
    }
    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        transition: transform 0.2s;
    }
    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }
</style>